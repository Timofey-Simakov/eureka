version: "2"
sql:
  - schema: "api/migrations"
    queries: "api/queries"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "api/internal/db"
        emit_json_tags: true
